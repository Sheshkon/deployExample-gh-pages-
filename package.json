{
    "source": "index.html",
    "scripts": {
        "start": "parcel",
        "build": "parcel --no-hmr --public-url ./",
        "deploy": "gh-pages -d dist",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "gh-pages": "^4.0.0",
        "parcel": "^2.8.2",
        "typescript": "^4.9.4"
    }
}