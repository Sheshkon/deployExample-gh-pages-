{
    "source": "index.html",
    "scripts": {
        "start": "parcel",
        "build": "parcel --no-hmr --public-url ./",
        "test": "echo \"Error: no test specified\" && exit 1"
    },

    "dependencies": {

        "parcel": "^2.8.2",
        "typescript": "^4.9.4"

    }

}